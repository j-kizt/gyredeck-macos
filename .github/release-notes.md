Agent Activity is a local macOS menu-bar companion that surfaces live Claude Code / Codex activity, provider usage, local services, and the state of the GitHub repos you track.

### What's new
- **Footer on every tab** showing the copyright on the left and the app version on the right.
- Copyright is also set in the app's macOS bundle metadata (visible in Finder → Get Info).
